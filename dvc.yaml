stages:
  train:
    cmd: python src/train.py
    deps:
      - src/train.py
      - data/processed
    params:
      - train.epochs
      - train.batch_size
      - train.imgsz
      - train.lr0
      - train.lrf
      - train.optimizer
      - train.data_yaml
    outs:
      - model/model_v${data_version}.pt
    metrics:
      - metrics/results.json:
          cache: false

  evaluate:
    cmd: python src/evaluate.py
    deps:
      - model/model_v${data_version}.pt
      - src/evaluate.py
    params:
      - train.data_yaml
    metrics:
      - metrics/eval.json:
          cache: false
  select_best_model:
    cmd: python src/select_best_model.py
    deps:
      - metrics/results.json
      - model/best.pt
      - model/model_v${data_version}.pt
      - src/select_best_model.py